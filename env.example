# ===== Node (LTS server) =====
PORT=5179
BIND_HOST=127.0.0.1
NODE_ENV=development

# INGEST_STREAM_KEY=lts:match:ingest

STAFF_ID=admin
STAFF_PW=admin1234
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d

REDIS_URL=redis://127.0.0.1:6379
# 발행 보드: match 페이지에서 발행한 경기 집계는 Redis board:published 키에 저장되며, /display 페이지가 WS로 수신

# Firebase Admin (Node) — prod에서는 값 채워져 있음
# FIREBASE_SERVICE_ACCOUNT_PATH=C:\LaserGame\secrets\firebase-service-account.json
FIREBASE_PROJECT_ID=your-project-id
# 경기 기록 Redis → Firestore 동기화 주기 (기본 10분)
# FIREBASE_SYNC_INTERVAL_MS=600000
# Firestore 컬렉션 이름 (기본 matches)
# FIREBASE_MATCHES_COLLECTION=matches

# ===== C# (ingest-bridge). Shares .env with Node. =====
# 실행: dotnet run --project ingest-bridge 또는 dotnet run --project ingest-bridge -- "<arena.data 경로>"
# Node 서버 npm run dev 시 브릿지는 자동으로 띄움 (LITEDB_ARENA_PATH 사용).
# LiteDB 두 종류 구분:
#   - arena: 경기 기록 (ingest-bridge가 Redis Stream으로 전송하는 소스)
#   - player: 유저 승패/프로필 기록 (별도 용도)
# LITEDB_ARENA_PATH=C:\LaserGame\Data\arena.data
# LITEDB_PLAYER_PATH=C:\LaserGame\Data\player.data
# LITEDB_PASSWORD=
# GAME_COLLECTION_NAME=game
# POLL_INTERVAL_MS=2000
# 게스트 API (Node가 비회원 등록 시 호출). ingest-bridge가 LITEDB_PLAYER_PATH 설정 시 기동함.
# GUEST_API_URL=http://127.0.0.1:5099
# GUEST_API_PORT=5099
